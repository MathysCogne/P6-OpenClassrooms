{
    "version": 2,
    "name": "tuto",
    "builds": [
      { "installCommand": "npm install", "src": "server.js", "use": "@vercel/node" }
    ],
    "routes": [
      { "src": "/(.*)", "dest": "/server.js" }
    ]
}